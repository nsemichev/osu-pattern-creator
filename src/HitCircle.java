public class HitCircle extends HitObject{
    HitCircle(int x, int y, int time, int type, int hitSound, String objectParams, String hitSample){
        super(x, y, time, type, hitSound, objectParams, hitSample);
    }

    public String toString(){
        return super.toString();
    }
}
